rescan cli:
. .bashrc
source .bashrc
source .bash_profile

export PATH=/Users/usr1/opt/app1/bin:$PATH

recurs:
mkdir -p

insesitivly case + recursivly + only match group:
grep -iro pattern path

tldr



grep -irol .gitconfig / 2>/dev/null
